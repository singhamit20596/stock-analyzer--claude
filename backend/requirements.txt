fastapi
uvicorn
sqlalchemy
pydantic
httpx
python-multipart

# OCR — RapidOCR is the engine actually used for screenshot ingestion.
pillow
numpy
rapidocr-onnxruntime

# Optional OCR fallbacks. ocr_engine.py tries RapidOCR first and degrades to
# these only if it is unavailable, so they are not installed by default —
# easyocr alone pulls in PyTorch (~2GB). Install by hand if you ever need them:
#   pip install easyocr pytesseract
anthropic
